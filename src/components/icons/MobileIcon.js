import React from 'react';
import { FaMobileAlt } from 'react-icons/fa';

const MobileIcon = ({ size = 24, color = "#10b981" }) => (
  <FaMobileAlt size={size} color={color} />
);

export default MobileIcon;